package by.epam.carsharing.model.entity;

/**
 * <P>Any class, implementing this interface must have id field in it.
 * This id is used for identifying the object in any storage where it is stored.</P>
 */
public interface Identifiable {
    int getId();
}
